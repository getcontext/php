{% extends 'SchoolAppBundle::front.html.twig' %}

{% block content %}
        <!--columns-->
        <div class="content_subpage">
        
	        <div class="column_l_dbl">
	        <h1>Login</h1>

		    {% if error %}
		        <div class="error">{{ error.message }}</div>
		    {% endif %}

    <form action="{{ path("_security_check") }}" method="post" id="login">
        <div>
            <label for="username">Username</label>
            <input type="text" id="email" name="_email" value="{{ last_username }}" />
        </div>

        <div>
            <label for="password">Password</label>
            <input type="password" id="password" name="_password" />
        </div>

        <input type="submit" class="symfony-button-grey" value="LOGIN" />
    </form>
 	  	</div><!--end column_l_dbl-->
        
      <div class="column_r">
       		<div class="blue_box">
                <h3>Dell</h3>
                <p class="white">Producent komputerów, monitorów, laptopów, urządzeń biurowych, akcesoriów komputerowych. Producent komputerów, monitorów, laptopów, urządzeń biurowych, akcesoriów komputerowych.</p>
                <p class="white">Producent komputerów, monitorów, laptopów, urządzeń biurowych, akcesoriów komputerowych.</p>
                <p> www.dell.pl</p>
            </div><!--end blue box--><!--end blue box-->
            
        </div><!--end column_r-->
        
        <div class="clr"></div><!--end columns-->
        </div><!--end content-->
  </div><!--end frame-->
</div><!--end mid_bgr-->
<!------------------------------------------------------------------------------------------------------->
{% endblock %}

{% set code = code(_self) %}
